const Database=require('better-sqlite3');const path=require('path');const db=new Database(path.join(__dirname,'school.db'));db.pragma('journal_mode=WAL');db.exec(`CREATE TABLE IF NOT EXISTS employee_requests(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,school_id INTEGER,request_type TEXT NOT NULL,title TEXT NOT NULL,description TEXT, status TEXT NOT NULL DEFAULT 'pending',manager_note TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS employee_attendance(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,school_id INTEGER,work_date TEXT NOT NULL,check_in TEXT,check_out TEXT,status TEXT DEFAULT 'present',note TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,UNIQUE(user_id,work_date),FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);`);