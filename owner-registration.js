const Module=require('module');const Database=require('better-sqlite3');const path=require('path');const previous=Module._load;const db=new Database(path.join(__dirname,'school.db'));db.pragma('journal_mode=WAL');
function columns(t){return db.prepare(`PRAGMA table_info(${t})`).all().map(x=>x.name)}function add(t,c,d){if(!columns(t).includes(c))try{db.exec(`ALTER TABLE ${t} ADD COLUMN ${c} ${d}`)}catch(e){console.error('REG_MIGRATION',t,c,e.message)}}
db.exec(`CREATE TABLE IF NOT EXISTS registration_requests(id INTEGER PRIMARY KEY AUTOINCREMENT,request_type TEXT NOT NULL DEFAULT 'user',user_id INTEGER,school_id INTEGER,full_name TEXT,email TEXT,phone TEXT,role TEXT,details TEXT,status TEXT NOT NULL DEFAULT 'pending',owner_note TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS school_access_requests(id INTEGER PRIMARY KEY AUTOINCREMENT,school_id INTEGER NOT NULL,manager_user_id INTEGER NOT NULL,status TEXT DEFAULT 'pending',owner_note TEXT,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP);`);
for(const [c,d] of [['request_type',"TEXT NOT NULL DEFAULT 'user'"],['user_id','INTEGER'],['school_id','INTEGER'],['full_name','TEXT'],['email','TEXT'],['phone','TEXT'],['role','TEXT'],['details','TEXT'],['status',"TEXT NOT NULL DEFAULT 'pending'"],['owner_note','TEXT'],['created_at','TEXT DEFAULT CURRENT_TIMESTAMP'],['updated_at','TEXT DEFAULT CURRENT_TIMESTAMP']])add('registration_requests',c,d);for(const [c,d] of [['school_id','INTEGER'],['manager_user_id','INTEGER'],['status',"TEXT DEFAULT 'pending'"],['owner_note','TEXT'],['created_at','TEXT DEFAULT CURRENT_TIMESTAMP'],['updated_at','TEXT DEFAULT CURRENT_TIMESTAMP']])add('school_access_requests',c,d);
function owner(req){return ['owner','مالك المنصة','مبرمج','مبرمج النظام'].includes(String(req.user?.role||req.enhancedUser?.role||''))}function schoolAdmin(req){return ['مدير','مدير المدرسة','وكيل المدرسة'].includes(String(req.user?.role||''))&&Number(req.user?.school_id)>0}
function install(app){if(app.__registrationRequestsInstalled)return;app.__registrationRequestsInstalled=true;const guard=(req,res,next)=>owner(req)?next():res.status(403).json({success:false,message:'غير مصرح'});const sg=(req,res,next)=>schoolAdmin(req)?next():res.status(403).json({success:false,message:'هذه المنطقة مخصصة لإدارة المدرسة'});
const normalize=()=>{db.prepare("UPDATE registration_requests SET status='pending' WHERE lower(trim(status)) IN ('new','review','قيد المراجعة','جديد')").run()};
app.get('/api/owner/registration-requests',guard,(req,res)=>{try{normalize();const rows=db.prepare(`SELECT r.*,u.username,u.active,s.name school_name FROM registration_requests r LEFT JOIN users u ON u.id=r.user_id LEFT JOIN schools s ON s.id=r.school_id ORDER BY CASE WHEN r.status='pending' THEN 0 ELSE 1 END,r.id DESC`).all();res.json({success:true,requests:rows,count:rows.filter(x=>x.status==='pending').length,pending_count:rows.filter(x=>x.status==='pending').length})}catch(e){console.error('REG_LIST',e);res.status(500).json({success:false,message:'تعذر تحميل طلبات التسجيل',error:e.message})}});
app.get('/api/owner/registration-requests/count',guard,(req,res)=>{normalize();const c=db.prepare("SELECT COUNT(*) c FROM registration_requests WHERE status='pending'").get().c;res.json({success:true,count:c})});
function review(req,res,decision){try{const r=db.prepare('SELECT * FROM registration_requests WHERE id=?').get(req.params.id);if(!r)return res.status(404).json({success:false,message:'الطلب غير موجود'});if(['new','review'].includes(String(r.status).toLowerCase()))db.prepare("UPDATE registration_requests SET status='pending' WHERE id=?").run(r.id);const current=db.prepare('SELECT * FROM registration_requests WHERE id=?').get(r.id);if(current.status!=='pending')return res.json({success:true,message:'الطلب تمت معالجته مسبقاً',status:current.status});const note=req.body?.note||req.body?.decision_note||(decision==='approved'?'تم الاعتماد':'تم رفض الطلب');const tx=db.transaction(()=>{if(decision==='approved'){if(current.user_id)db.prepare('UPDATE users SET active=1,must_change_password=1 WHERE id=?').run(current.user_id);if(current.school_id&&current.request_type==='school'){const sc=columns('schools');if(sc.includes('active'))db.prepare('UPDATE schools SET active=1 WHERE id=?').run(current.school_id);if(sc.includes('status'))db.prepare("UPDATE schools SET status='active' WHERE id=?").run(current.school_id);if(sc.includes('owner_approved'))db.prepare('UPDATE schools SET owner_approved=1 WHERE id=?').run(current.school_id)}if(current.school_id&&current.request_type==='school'&&current.user_id)db.prepare("UPDATE users SET role='مدير',active=1,school_id=? WHERE id=?").run(current.school_id,current.user_id)}else{if(current.user_id)db.prepare('UPDATE users SET active=0 WHERE id=?').run(current.user_id);if(current.school_id&&current.request_type==='school'){const sc=columns('schools');if(sc.includes('status'))db.prepare("UPDATE schools SET status='rejected' WHERE id=?").run(current.school_id);if(sc.includes('owner_approved'))db.prepare('UPDATE schools SET owner_approved=0 WHERE id=?').run(current.school_id)}}db.prepare('UPDATE registration_requests SET status=?,owner_note=?,updated_at=CURRENT_TIMESTAMP WHERE id=?').run(decision,note,current.id);if(current.request_type==='school'&&current.school_id&&current.user_id)db.prepare('UPDATE school_access_requests SET status=?,owner_note=?,updated_at=CURRENT_TIMESTAMP WHERE school_id=? AND manager_user_id=?').run(decision,note,current.school_id,current.user_id)});tx();res.json({success:true,message:decision==='approved'?'تم قبول الطلب وتفعيل الحساب وربطه بالمدرسة':'تم رفض الطلب',status:decision})}catch(e){console.error('REG_REVIEW',e);res.status(500).json({success:false,message:'تعذر تنفيذ مراجعة الطلب',error:e.message})}}
app.post('/api/owner/registration-requests/:id/approve',guard,(req,res)=>review(req,res,'approved'));app.post('/api/owner/registration-requests/:id/reject',guard,(req,res)=>review(req,res,'rejected'));app.post('/api/owner/registration-requests/:id/review',guard,(req,res)=>{const raw=String(req.body?.decision||req.body?.action||req.body?.status||'').toLowerCase();return review(req,res,['approve','approved','accept','accepted','قبول','مقبول'].includes(raw)?'approved':['reject','rejected','decline','declined','رفض','مرفوض'].includes(raw)?'rejected':null)||null});
app.get('/api/school/registration-requests',sg,(req,res)=>{try{const rows=db.prepare(`SELECT r.*,u.username,u.active,s.name school_name FROM registration_requests r LEFT JOIN users u ON u.id=r.user_id LEFT JOIN schools s ON s.id=r.school_id WHERE r.school_id=? AND r.request_type='user' ORDER BY CASE WHEN r.status='pending' THEN 0 ELSE 1 END,r.id DESC`).all(req.user.school_id);res.json({success:true,requests:rows,count:rows.filter(x=>x.status==='pending').length})}catch(e){res.status(500).json({success:false,message:'تعذر تحميل طلبات الموظفين',error:e.message})}});
app.post('/api/school/registration-requests/:id/approve',sg,(req,res)=>{const r=db.prepare("SELECT * FROM registration_requests WHERE id=? AND school_id=? AND request_type='user'").get(req.params.id,req.user.school_id);if(!r)return res.status(404).json({success:false,message:'طلب الموظف غير موجود'});if(r.status!=='pending')return res.json({success:true,message:'الطلب تمت معالجته مسبقاً'});const tx=db.transaction(()=>{if(r.user_id)db.prepare('UPDATE users SET active=1,must_change_password=1 WHERE id=? AND school_id=?').run(r.user_id,req.user.school_id);db.prepare("UPDATE registration_requests SET status='approved',owner_note=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").run(req.body?.note||'تم اعتماد الموظف',r.id)});tx();res.json({success:true,message:'تم اعتماد الموظف ويمكنه تسجيل الدخول'})});
app.post('/api/school/registration-requests/:id/reject',sg,(req,res)=>{const r=db.prepare("SELECT * FROM registration_requests WHERE id=? AND school_id=? AND request_type='user'").get(req.params.id,req.user.school_id);if(!r)return res.status(404).json({success:false,message:'طلب الموظف غير موجود'});db.prepare('UPDATE users SET active=0 WHERE id=? AND school_id=?').run(r.user_id,req.user.school_id);db.prepare("UPDATE registration_requests SET status='rejected',owner_note=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").run(req.body?.note||'تم رفض الطلب',r.id);res.json({success:true,message:'تم رفض طلب الموظف'})});
}
Module._load=function(request,parent,isMain){const loaded=previous.apply(this,arguments);if(request!=='express')return loaded;const wrapped=function(){const app=loaded();const originalUse=app.use.bind(app);app.use=function(...args){const out=originalUse(...args);const p=args[0];if(p==='/api/owner'||args.slice(typeof p==='string'?1:0).some(x=>typeof x==='function'&&x.name==='auth'))install(app);return out};return app};Object.setPrototypeOf(wrapped,loaded);wrapped.prototype=loaded.prototype;for(const k of Object.keys(loaded))wrapped[k]=loaded[k];return wrapped};