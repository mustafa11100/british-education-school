const express = require("express"); const path = require("path"); const Database = require("better-sqlite3"); const app = express(); const db = new Database("school.db"); app.use(express.json()); app.use(express.static(__dirname)); /* ========================================================= DATABASE ========================================================= */ db.exec(` CREATE TABLE IF NOT EXISTS users ( id INTEGER PRIMARY KEY AUTOINCREMENT, username TEXT UNIQUE NOT NULL, password TEXT NOT NULL, role TEXT NOT NULL, name TEXT NOT NULL, active INTEGER DEFAULT 1, last_seen TEXT ); CREATE TABLE IF NOT EXISTS students ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, class_name TEXT, parent_phone TEXT, status TEXT DEFAULT 'حاضر' ); CREATE TABLE IF NOT EXISTS notes ( id INTEGER PRIMARY KEY AUTOINCREMENT, student_id INTEGER, text TEXT NOT NULL, created_at TEXT ); CREATE TABLE IF NOT EXISTS videos ( id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, file_name TEXT, created_at TEXT ); /* حضور الطلاب */ CREATE TABLE IF NOT EXISTS student_attendance ( id INTEGER PRIMARY KEY AUTOINCREMENT, student_id INTEGER NOT NULL, date TEXT NOT NULL, status TEXT NOT NULL, created_at TEXT, UNIQUE(student_id, date) ); /* الموظفون */ CREATE TABLE IF NOT EXISTS employees ( id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, employee_number TEXT UNIQUE, name TEXT NOT NULL, job_title TEXT, department TEXT, phone TEXT, hire_date TEXT, basic_salary REAL DEFAULT 0, allowance REAL DEFAULT 0, active INTEGER DEFAULT 1, created_at TEXT ); /* حضور الموظفين */ CREATE TABLE IF NOT EXISTS employee_attendance ( id INTEGER PRIMARY KEY AUTOINCREMENT, employee_id INTEGER NOT NULL, date TEXT NOT NULL, check_in TEXT, check_out TEXT, status TEXT NOT NULL, late_minutes INTEGER DEFAULT 0, created_at TEXT, UNIQUE(employee_id, date) ); /* الخصومات */ CREATE TABLE IF NOT EXISTS deductions ( id INTEGER PRIMARY KEY AUTOINCREMENT, employee_id INTEGER NOT NULL, amount REAL NOT NULL, reason TEXT NOT NULL, date TEXT NOT NULL, month TEXT NOT NULL, automatic INTEGER DEFAULT 0, created_by INTEGER, created_at TEXT ); /* المكافآت */ CREATE TABLE IF NOT EXISTS bonuses ( id INTEGER PRIMARY KEY AUTOINCREMENT, employee_id INTEGER NOT NULL, amount REAL NOT NULL, reason TEXT NOT NULL, date TEXT NOT NULL, month TEXT NOT NULL, created_by INTEGER, created_at TEXT ); /* إعدادات خصومات الحضور */ CREATE TABLE IF NOT EXISTS payroll_settings ( id INTEGER PRIMARY KEY CHECK (id = 1), absence_deduction REAL DEFAULT 0, late_deduction REAL DEFAULT 0, allowed_late_minutes INTEGER DEFAULT 15 ); /* سجل العمليات */ CREATE TABLE IF NOT EXISTS audit_logs ( id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, action TEXT NOT NULL, details TEXT, created_at TEXT ); `); /* ========================================================= DEFAULT PAYROLL SETTINGS ========================================================= */ const settings = db .prepare("SELECT * FROM payroll_settings WHERE id = 1") .get(); if (!settings) { db.prepare(` INSERT INTO payroll_settings ( id, absence_deduction, late_deduction, allowed_late_minutes ) VALUES (1, 300, 50, 15) `).run(); } /* ========================================================= COMPATIBILITY ========================================================= */ try { db.prepare( "ALTER TABLE users ADD COLUMN active INTEGER DEFAULT 1" ).run(); } catch (e) {} try { db.prepare( "ALTER TABLE users ADD COLUMN last_seen TEXT" ).run(); } catch (e) {} /* ========================================================= DEFAULT USERS ========================================================= */ const userCount = db .prepare("SELECT COUNT(*) AS count FROM users") .get().count; if (userCount === 0) { const insertUser = db.prepare(` INSERT INTO users ( username, password, role, name, active, last_seen ) VALUES (?, ?, ?, ?, 1, NULL) `); insertUser.run( "admin", "1234", "admin", "مدير المدرسة" ); insertUser.run( "teacher", "1234", "teacher", "الأستاذ أحمد" ); insertUser.run( "parent", "1234", "parent", "ولي أمر محمد أحمد" ); insertUser.run( "student", "1234", "student", "محمد أحمد" ); } /* ========================================================= DEFAULT STUDENT ========================================================= */ const studentCount = db .prepare("SELECT COUNT(*) AS count FROM students") .get().count; if (studentCount === 0) { db.prepare(` INSERT INTO students ( name, class_name, parent_phone, status ) VALUES (?, ?, ?, ?) `).run( "محمد أحمد", "الصف السادس", "201000000000", "حاضر" ); } /* ========================================================= HELPERS ========================================================= */ function nowISO() { return new Date().toISOString(); } function today() { return new Date().toISOString().slice(0, 10); } function currentMonth() { return new Date().toISOString().slice(0, 7); } function logAction(userId, action, details = "") { db.prepare(` INSERT INTO audit_logs ( user_id, action, details, created_at ) VALUES (?, ?, ?, ?) `).run( userId || null, action, details, nowISO() ); } /* ========================================================= LOGIN ========================================================= */ app.post("/api/login", (req, res) => { const { username, password } = req.body || {}; if (!username || !password) { return res.status(400).json({ error: "أدخل اسم المستخدم وكلمة المرور" }); } const user = db.prepare(` SELECT id, username, role, name, active, last_seen FROM users WHERE username = ? AND password = ? `).get(username, password); if (!user) { return res.status(401).json({ error: "بيانات الدخول غير صحيحة" }); } if (!user.active) { return res.status(403).json({ error: "هذا الحساب موقوف من الإدارة" }); } const now = nowISO(); db.prepare(` UPDATE users SET last_seen = ? WHERE id = ? `).run(now, user.id); user.last_seen = now; logAction( user.id, "LOGIN", `تسجيل دخول المستخدم ${user.username}` ); res.json(user); }); /* ========================================================= ACTIVITY ========================================================= */ app.post("/api/activity", (req, res) => { const { user_id } = req.body || {}; if (!user_id) { return res.status(400).json({ error: "user_id مطلوب" }); } const user = db.prepare(` SELECT id, active FROM users WHERE id = ? `).get(user_id); if (!user) { return res.status(404).json({ error: "المستخدم غير موجود" }); } if (!user.active) { return res.status(403).json({ error: "الحساب موقوف" }); } const now = nowISO(); db.prepare(` UPDATE users SET last_seen = ? WHERE id = ? `).run(now, user_id); res.json({ success: true, last_seen: now }); }); /* ========================================================= USERS ========================================================= */ app.get("/api/users", (req, res) => { const users = db.prepare(` SELECT id, username, role, name, active, last_seen FROM users ORDER BY id DESC `).all(); const now = Date.now(); const result = users.map(user => { let online = false; if (user.last_seen) { const last = new Date(user.last_seen).getTime(); online = user.active === 1 && now - last <= 60000; } return { ...user, online }; }); res.json(result); }); /* ========================================================= ADD USER ========================================================= */ app.post("/api/users", (req, res) => { const { username, password, role, name } = req.body || {}; if (!username || !password || !role || !name) { return res.status(400).json({ error: "أكمل جميع بيانات المستخدم" }); } try { const result = db.prepare(` INSERT INTO users ( username, password, role, name, active ) VALUES (?, ?, ?, ?, 1) `).run( username, password, role, name ); logAction( null, "ADD_USER", `إضافة المستخدم ${username}` ); res.json({ success: true, id: result.lastInsertRowid }); } catch (error) { res.status(400).json({ error: "اسم المستخدم موجود بالفعل" }); } }); /* ========================================================= DISABLE USER ========================================================= */ app.patch("/api/users/:id/disable", (req, res) => { const user = db.prepare(` SELECT id, username FROM users WHERE id = ? `).get(req.params.id); if (!user) { return res.status(404).json({ error: "المستخدم غير موجود" }); } if (user.username === "admin") { return res.status(400).json({ error: "لا يمكن إيقاف المدير الرئيسي" }); } db.prepare(` UPDATE users SET active = 0 WHERE id = ? `).run(req.params.id); logAction( null, "DISABLE_USER", `إيقاف المستخدم ${user.username}` ); res.json({ success: true, message: "تم إيقاف المستخدم" }); }); /* ========================================================= ENABLE USER ========================================================= */ app.patch("/api/users/:id/enable", (req, res) => { db.prepare(` UPDATE users SET active = 1 WHERE id = ? `).run(req.params.id); res.json({ success: true, message: "تم تفعيل المستخدم" }); }); /* ========================================================= CHANGE PASSWORD ========================================================= */ app.patch("/api/users/:id/password", (req, res) => { const { current_password, new_password } = req.body || {}; if (!new_password) { return res.status(400).json({ error: "أدخل كلمة المرور الجديدة" }); } const user = db.prepare(` SELECT * FROM users WHERE id = ? `).get(req.params.id); if (!user) { return res.status(404).json({ error: "المستخدم غير موجود" }); } if ( current_password !== undefined && current_password !== user.password ) { return res.status(400).json({ error: "كلمة المرور الحالية غير صحيحة" }); } db.prepare(` UPDATE users SET password = ? WHERE id = ? `).run( new_password, user.id ); logAction( user.id, "CHANGE_PASSWORD", `تغيير كلمة مرور ${user.username}` ); res.json({ success: true, message: "تم تغيير كلمة المرور" }); }); /* ========================================================= DELETE USER ========================================================= */ app.delete("/api/users/:id", (req, res) => { const user = db.prepare(` SELECT id, username FROM users WHERE id = ? `).get(req.params.id); if (!user) { return res.status(404).json({ error: "المستخدم غير موجود" }); } if (user.username === "admin") { return res.status(400).json({ error: "لا يمكن حذف المدير الرئيسي" }); } db.prepare(` DELETE FROM users WHERE id = ? `).run(req.params.id); logAction( null, "DELETE_USER", `حذف المستخدم ${user.username}` ); res.json({ success: true, message: "تم حذف المستخدم" }); }); /* ========================================================= STUDENTS ========================================================= */ app.get("/api/students", (req, res) => { const students = db.prepare(` SELECT * FROM students ORDER BY id DESC `).all(); res.json(students); }); app.post("/api/students", (req, res) => { const { name, class_name, parent_phone } = req.body || {}; if (!name || !class_name || !parent_phone) { return res.status(400).json({ error: "أكمل بيانات الطالب" }); } const result = db.prepare(` INSERT INTO students ( name, class_name, parent_phone, status ) VALUES (?, ?, ?, 'حاضر') `).run( name, class_name, parent_phone ); res.json({ success: true, id: result.lastInsertRowid }); }); /* ========================================================= STUDENT ATTENDANCE ========================================================= */ app.patch("/api/students/:id/status", (req, res) => { const student = db.prepare(` SELECT * FROM students WHERE id = ? `).get(req.params.id); if (!student) { return res.status(404).json({ error: "الطالب غير موجود" }); } const newStatus = student.status === "حاضر" ? "غائب" : "حاضر"; const date = today(); db.prepare(` UPDATE students SET status = ? WHERE id = ? `).run( newStatus, student.id ); db.prepare(` INSERT INTO student_attendance ( student_id, date, status, created_at ) VALUES (?, ?, ?, ?) ON CONFLICT(student_id, date) DO UPDATE SET status = excluded.status `).run( student.id, date, newStatus, nowISO() ); res.json({ success: true, status: newStatus }); }); app.get("/api/students/:id/attendance", (req, res) => { const records = db.prepare(` SELECT * FROM student_attendance WHERE student_id = ? ORDER BY date DESC `).all(req.params.id); const present = records.filter( x => x.status === "حاضر" ).length; const absent = records.filter( x => x.status === "غائب" ).length; res.json({ records, present, absent, total: records.length, percentage: records.length ? Math.round((present / records.length) * 100) : 0 }); }); /* ========================================================= EMPLOYEES ========================================================= */ app.get("/api/employees", (req, res) => { const employees = db.prepare(` SELECT e.*, u.username, u.active AS account_active, u.last_seen FROM employees e LEFT JOIN users u ON u.id = e.user_id ORDER BY e.id DESC `).all(); res.json(employees); }); app.post("/api/employees", (req, res) => { const { employee_number, name, job_title, department, phone, hire_date, basic_salary, allowance, user_id } = req.body || {}; if (!name) { return res.status(400).json({ error: "اسم الموظف مطلوب" }); } try { const result = db.prepare(` INSERT INTO employees ( user_id, employee_number, name, job_title, department, phone, hire_date, basic_salary, allowance, created_at ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?) `).run( user_id || null, employee_number || null, name, job_title || "", department || "", phone || "", hire_date || "", Number(basic_salary || 0), Number(allowance || 0), nowISO() ); logAction( null, "ADD_EMPLOYEE", `إضافة الموظف ${name}` ); res.json({ success: true, id: result.lastInsertRowid }); } catch (error) { res.status(400).json({ error: "رقم الموظف موجود بالفعل" }); } }); /* ========================================================= EMPLOYEE ATTENDANCE ========================================================= */ app.post("/api/employees/:id/attendance", (req, res) => { const employee = db.prepare(` SELECT * FROM employees WHERE id = ? `).get(req.params.id); if (!employee) { return res.status(404).json({ error: "الموظف غير موجود" }); } const { check_in, check_out } = req.body || {}; const date = today(); const settings = db .prepare("SELECT * FROM payroll_settings WHERE id = 1") .get(); let lateMinutes = 0; let status = "حاضر"; if (check_in) { const start = 8 * 60; const [hour, minute] = check_in.split(":").map(Number); const arrival = hour * 60 + minute; lateMinutes = Math.max(0, arrival - start); if ( lateMinutes > settings.allowed_late_minutes ) { status = "متأخر"; } } db.prepare(` INSERT INTO employee_attendance ( employee_id, date, check_in, check_out, status, late_minutes, created_at ) VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT(employee_id, date) DO UPDATE SET check_in = excluded.check_in, check_out = excluded.check_out, status = excluded.status, late_minutes = excluded.late_minutes `).run( employee.id, date, check_in || null, check_out || null, status, lateMinutes, nowISO() ); res.json({ success: true, status, late_minutes: lateMinutes }); }); /* ========================================================= DEDUCTIONS ========================================================= */ app.get("/api/employees/:id/deductions", (req, res) => { const deductions = db.prepare(` SELECT * FROM deductions WHERE employee_id = ? ORDER BY date DESC, id DESC `).all(req.params.id); res.json(deductions); }); app.post("/api/employees/:id/deductions", (req, res) => { const { amount, reason, month } = req.body || {}; if (!amount || !reason) { return res.status(400).json({ error: "قيمة الخصم وسببه مطلوبان" }); } const result = db.prepare(` INSERT INTO deductions ( employee_id, amount, reason, date, month, automatic, created_at ) VALUES (?, ?, ?, ?, ?, 0, ?) `).run( req.params.id, Number(amount), reason, today(), month || currentMonth(), nowISO() ); res.json({ success: true, id: result.lastInsertRowid }); }); /* ========================================================= DELETE DEDUCTION ========================================================= */ app.delete("/api/deductions/:id", (req, res) => { const deduction = db.prepare(` SELECT * FROM deductions WHERE id = ? `).get(req.params.id); if (!deduction) { return res.status(404).json({ error: "الخصم غير موجود" }); } db.prepare(` DELETE FROM deductions WHERE id = ? `).run(req.params.id); logAction( null, "DELETE_DEDUCTION", `حذف خصم بقيمة ${deduction.amount} بسبب ${deduction.reason}` ); res.json({ success: true, message: "تم حذف الخصم" }); }); /* ========================================================= BONUSES ========================================================= */ app.get("/api/employees/:id/bonuses", (req, res) => { const bonuses = db.prepare(` SELECT * FROM bonuses WHERE employee_id = ? ORDER BY date DESC, id DESC `).all(req.params.id); res.json(bonuses); }); app.post("/api/employees/:id/bonuses", (req, res) => { const { amount, reason, month } = req.body || {}; if (!amount || !reason) { return res.status(400).json({ error: "قيمة المكافأة وسببها مطلوبان" }); } const result = db.prepare(` INSERT INTO bonuses ( employee_id, amount, reason, date, month, created_at ) VALUES (?, ?, ?, ?, ?, ?) `).run( req.params.id, Number(amount), reason, today(), month || currentMonth(), nowISO() ); res.json({ success: true, id: result.lastInsertRowid }); }); /* ========================================================= DELETE BONUS ========================================================= */ app.delete("/api/bonuses/:id", (req, res) => { const bonus = db.prepare(` SELECT * FROM bonuses WHERE id = ? `).get(req.params.id); if (!bonus) { return res.status(404).json({ error: "المكافأة غير موجودة" }); } db.prepare(` DELETE FROM bonuses WHERE id = ? `).run(req.params.id); logAction( null, "DELETE_BONUS", `حذف مكافأة بقيمة ${bonus.amount}` ); res.json({ success: true, message: "تم حذف المكافأة" }); }); /* ========================================================= PAYROLL ========================================================= */ app.get("/api/employees/:id/payroll", (req, res) => { const month = req.query.month || currentMonth(); const employee = db.prepare(` SELECT * FROM employees WHERE id = ? `).get(req.params.id); if (!employee) { return res.status(404).json({ error: "الموظف غير موجود" }); } const bonuses = db.prepare(` SELECT COALESCE(SUM(amount), 0) AS total FROM bonuses WHERE employee_id = ? AND month = ? `).get( employee.id, month ).total; const deductions = db.prepare(` SELECT COALESCE(SUM(amount), 0) AS total FROM deductions WHERE employee_id = ? AND month = ? `).get( employee.id, month ).total; const gross = Number(employee.basic_salary || 0) + Number(employee.allowance || 0) + Number(bonuses || 0); const net = gross - Number(deductions || 0); res.json({ month, employee, basic_salary: Number(employee.basic_salary || 0), allowance: Number(employee.allowance || 0), bonuses: Number(bonuses || 0), deductions: Number(deductions || 0), gross, net }); }); /* ========================================================= PAYROLL SETTINGS ========================================================= */ app.get("/api/payroll-settings", (req, res) => { const settings = db .prepare("SELECT * FROM payroll_settings WHERE id = 1") .get(); res.json(settings); }); app.patch("/api/payroll-settings", (req, res) => { const { absence_deduction, late_deduction, allowed_late_minutes } = req.body || {}; db.prepare(` UPDATE payroll_settings SET absence_deduction = ?, late_deduction = ?, allowed_late_minutes = ? WHERE id = 1 `).run( Number(absence_deduction || 0), Number(late_deduction || 0), Number(allowed_late_minutes || 0) ); res.json({ success: true, message: "تم تحديث إعدادات الرواتب" }); }); /* ========================================================= AUDIT LOGS ========================================================= */ app.get("/api/audit-logs", (req, res) => { const logs = db.prepare(` SELECT a.*, u.name AS user_name, u.username FROM audit_logs a LEFT JOIN users u ON u.id = a.user_id ORDER BY a.id DESC LIMIT 500 `).all(); res.json(logs); }); /* ========================================================= NOTES ========================================================= */ app.post("/api/notes", (req, res) => { const { student_id, text } = req.body || {}; if (!student_id || !text) { return res.status(400).json({ error: "أدخل الملاحظة" }); } const result = db.prepare(` INSERT INTO notes ( student_id, text, created_at ) VALUES (?, ?, ?) `).run( student_id, text, new Date().toLocaleString("ar-EG") ); res.json({ success: true, id: result.lastInsertRowid }); }); app.get("/api/notes/:studentId", (req, res) => { const notes = db.prepare(` SELECT * FROM notes WHERE student_id = ? ORDER BY id DESC `).all(req.params.studentId); res.json(notes); }); /* ========================================================= VIDEOS ========================================================= */ app.post("/api/videos", (req, res) => { const { title, file_name } = req.body || {}; if (!title) { return res.status(400).json({ error: "اكتب اسم الحصة" }); } const result = db.prepare(` INSERT INTO videos ( title, file_name, created_at ) VALUES (?, ?, ?) `).run( title, file_name || "", new Date().toLocaleString("ar-EG") ); res.json({ success: true, id: result.lastInsertRowid }); }); app.get("/api/videos", (req, res) => { const videos = db.prepare(` SELECT * FROM videos ORDER BY id DESC `).all(); res.json(videos); }); /* ========================================================= HEALTH ========================================================= */ app.get("/api/health", (req, res) => { res.json({ success: true, message: "School portal is running" }); }); /* ========================================================= START ========================================================= */ const PORT = process.env.PORT || 3000; app.listen( PORT, "0.0.0.0", () => { console.log( `School portal running on port ${PORT}` ); } );
