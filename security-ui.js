(()=>{const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const api=async(u,o={})=>{o.headers={...(o.headers||{}),'Content-Type':'application/json',...(window.S?.token?{Authorization:'Bearer '+window.S.token}:{})};const r=await fetch(u,o),d=await r.json();if(!r.ok)throw Error(d.message||'تعذر التنفيذ');return d};const add=()=>{const nav=$('#nav');if(!nav||nav.dataset.sec)return;nav.dataset.sec='1';const label=document.createElement('div');label.className='label';label.textContent='الأمان والمنصة';nav.appendChild(label);[['audit','🛡️','سجل التدقيق'],['backup','💾','النسخ الاحتياطية'],['2fa','🔐','المصادقة الثنائية']].forEach(([id,icon,text])=>{const b=document.createElement('button');b.className='navbtn';b.innerHTML=`<span class="icon">${icon}</span>${text}`;b.onclick=()=>view(id);nav.appendChild(b)})};const view=async type=>{const v=$('#view');try{if(type==='audit'){const d=await api('/api/security/audit');v.innerHTML=`<div class="head"><div><h2>سجل التدقيق</h2><p class="muted">جميع العمليات الحساسة المسجلة.</p></div></div><div class="panel"><div class="wrap"><table><thead><tr><th>المستخدم</th><th>العملية</th><th>التفاصيل</th><th>التاريخ</th></tr></thead><tbody>${d.logs.map(x=>`<tr><td>${esc(x.full_name||x.username||'—')}</td><td>${esc(x.action)}</td><td>${esc(x.details)}</td><td>${esc(x.created_at)}</td></tr>`).join('')}</tbody></table></div></div>`}else if(type==='backup'){const d=await api('/api/security/backups');v.innerHTML=`<div class="head"><div><h2>النسخ الاحتياطية</h2><p class="muted">إدارة نسخ قاعدة البيانات.</p></div><button onclick="window.makeEduBackup()">إنشاء نسخة الآن</button></div><div class="panel"><div class="wrap"><table><thead><tr><th>الملف</th><th>الحجم</th><th>التاريخ</th></tr></thead><tbody>${d.backups.map(x=>`<tr><td>${esc(x.file_name)}</td><td>${Number(x.size||0).toLocaleString('ar-EG')} بايت</td><td>${esc(x.created_at)}</td></tr>`).join('')}</tbody></table></div></div>`}else{v.innerHTML=`<div class="head"><div><h2>المصادقة الثنائية</h2><p class="muted">حماية الحسابات الحساسة بتطبيق Authenticator.</p></div></div><div class="panel"><p>ابدأ إعداد المصادقة الثنائية من هنا، ثم أدخل الرمز لإتمام التفعيل.</p><button onclick="window.startEdu2FA()">بدء الإعداد</button><div id="twofaBox"></div></div>`}}catch(e){v.innerHTML=`<div class="panel notice">${esc(e.message)}</div>`}};window.makeEduBackup=async()=>{try{const d=await api('/api/security/backup',{method:'POST'});alert(d.message);view('backup')}catch(e){alert(e.message)}};window.startEdu2FA=async()=>{try{const d=await api('/api/security/2fa/setup',{method:'POST'});$('#twofaBox').innerHTML=`<div class="educore-meta">Secret: <b>${esc(d.secret)}</b><br>Account: ${esc(d.account)}<br><label>رمز Authenticator</label><input id="eduOtp" inputmode="numeric" maxlength="6"><button onclick="window.enableEdu2FA()">تفعيل</button></div>`}catch(e){alert(e.message)}};window.enableEdu2FA=async()=>{try{const d=await api('/api/security/2fa/enable',{method:'POST',body:JSON.stringify({otp:$('#eduOtp').value})});alert(d.message)}catch(e){alert(e.message)}};const t=setInterval(()=>{if($('#nav')&&window.S?.user){clearInterval(t);add()}},300)})();
